define hostgroup {
        hostgroup_name  all
		alias           All Servers
		members         *
        }
define hostgroup {
        hostgroup_name  http-balanceador
		alias           HTTP servers
		members         lb
        }
define hostgroup {
        hostgroup_name  http-cdpsfy
		alias           HTTP servers
		members         s1
        }

define hostgroup {
        hostgroup_name  http-tracks
		alias           HTTP servers
		members         s2,s3,s4
        }

